# PixelArt 2026

PixelArt 2026, sanatçıların eserlerini dijital ortamda sergilemeleri ve yarışmalara katılmaları için geliştirilmiş modern bir başvuru platformudur. Bu proje, sanatçıların eserlerini (PDF, JPG, PNG formatlarında) kolayca yüklemelerine ve başvurularının durumunu takip etmelerine olanak tanır.

## 🚀 Proje Amacı

Sanatçıların dijital portfolyolarını veya yarışma başvurularını güvenli, hızlı ve kullanıcı dostu bir arayüz üzerinden gerçekleştirmelerini sağlamak. Sistem, başvuruları veritabanına kaydeder, dosyaları bulut depolama alanında saklar ve sanatçıya otomatik bilgilendirme e-postası gönderir.

## 🛠 Kullanılan Teknolojiler

### Frontend
- **Framework:** Next.js 16 (App Router)
- **Dil:** TypeScript
- **Stil:** Tailwind CSS
- **Form Yönetimi:** React Hooks
- **HTTP İstemcisi:** Fetch API

### Backend
- **Platform:** Node.js
- **Framework:** Express.js
- **Veritabanı & Depolama:** Supabase
- **E-posta Servisi:** Nodemailer (SMTP / Mailtrap)
- **Dosya Yönetimi:** Multer

## 📦 Kurulum ve Çalıştırma

Projeyi yerel ortamınızda çalıştırmak için aşağıdaki adımları izleyin.

### Ön Hazırlık

1. Projeyi klonlayın:
   ```bash
   git clone <repo-url>
   cd pixelart-2026
   ```

### 1. Backend Kurulumu

Backend klasörüne gidin ve bağımlılıkları yükleyin:

```bash
cd backend
npm install
```

`.env` dosyasını oluşturun:
`backend` klasörü içinde `.env` adında bir dosya oluşturun ve aşağıdaki değişkenleri ekleyin:

```env
PORT=5001
SUPABASE_URL=https://sizin-supabase-url.supabase.co
SUPABASE_KEY=sizin-supabase-service-role-key
EMAIL_HOST=smtp.mailtrap.io (veya kullandığınız SMTP sunucusu)
EMAIL_PORT=2525
EMAIL_USER=sizin-mailtrap-kullanici-adiniz
EMAIL_PASS=sizin-mailtrap-sifreniz
```

Backend sunucusunu başlatın:

```bash
npm run dev
```
Sunucu `http://localhost:5001` adresinde çalışacaktır.

### 2. Frontend Kurulumu

Yeni bir terminal açın, frontend klasörüne gidin ve bağımlılıkları yükleyin:

```bash
cd frontend
npm install
```

`.env.local` dosyasını oluşturun:
`frontend` klasörü içinde `.env.local` adında bir dosya oluşturun:

```env
NEXT_PUBLIC_API_URL=http://localhost:5001
```

Frontend uygulamasını başlatın:

```bash
npm run dev
```
Uygulama `http://localhost:3000` adresinde çalışacaktır.

## ⚙️ Yapılandırma

### Supabase Ayarları

1.  **Storage:**
    *   `artworks` adında bir "Public" bucket oluşturun.
    *   Bu bucket içinde `pdf-application`, `jpg-application`, `png-application` ve `others` klasör yapısını destekleyecek şekilde ayarlanır (kod otomatik klasörleme yapar).

2.  **Database:**
    *   SQL Editöründe aşağıdaki tabloyu oluşturun:

    ```sql
    create table submissions (
      id bigint generated by default as identity primary key,
      created_at timestamp with time zone default timezone('utc'::text, now()) not null,
      artist_name text,
      email text,
      description text,
      public_url text
    );
    ```

### Mailtrap Ayarları

1.  [Mailtrap.io](https://mailtrap.io) üzerinde bir hesap oluşturun.
2.  "Inboxes" bölümünden projeniz için bir inbox oluşturun.
3.  "SMTP Settings" sekmesinden Host, Port, Username ve Password bilgilerini alıp `backend/.env` dosyasına ekleyin.

## 📂 Mimari Şema

```
pixelart-2026/
├── backend/                # Express API Sunucusu
│   ├── config/            # Yapılandırma dosyaları (Supabase vb.)
│   ├── routes/            # API rotaları (submissionRoutes)
│   ├── index.js           # Sunucu giriş noktası
│   ├── .env               # Ortam değişkenleri
│   └── package.json       # Backend bağımlılıkları
├── frontend/               # Next.js Uygulaması
│   ├── app/               # Sayfalar ve Layoutlar
│   ├── components/        # Yeniden kullanılabilir bileşenler (SubmissionForm)
│   ├── public/            # Statik dosyalar
│   ├── .env.local         # Frontend ortam değişkenleri
│   └── package.json       # Frontend bağımlılıkları
├── .gitignore
└── README.md              # Proje dökümantasyonu
```
